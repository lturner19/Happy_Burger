<img src="../img/10610510.jpg" alt="Happy Burger image">

<h1>EAT DA BURGER</h1>

<h3>Burgers not devoured</h3>


<ol>
  {{#each burgers}}
    {{#unless devour}}
      {{> burger-block}}
      {{!-- removed burgers/ --}}
    {{/unless}}
  {{/each}}
</ol>

<h3>Devoured burgers</h3>

<ol>
  {{#each burgers}}
    {{#if devour}}
      {{> burger-block}} 
      {{!-- removed burgers/ --}}
    {{/if}}
  {{/each}}
</ol>

<h2>Burger Name</h2> 
<form class="create-form">

  <div class="form-group">
    <textarea type="text" id="burger" name="name" rows="5" cols = "30"></textarea>
    <br>
    <br>
     <label for="burger" class="burger-name">Please add a burger you would like to devour.</label>
  </div>
  <br>
  <button type="submit" id="submit">Add</button>
</form>

{{!-- 
  <div class="form-group">
    <label for="eaten">Eaten?</label><br>
    <input type="radio" name="devoured" value="1" checked> Devoured!<br>
    <input type="radio" name="devoured" value="0"> Not Devoured!
  </div> --}}
